package ru.job4j.orderbook;

import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;

/**
 * Task TO DO
 *
 * @author Dmitriy Bobrov (bobrov.dmitriy@gmail.com)
 */
public class Book {
    private Map<Integer, Order> book1 = new HashMap<>();
    private Map<Integer, Order> book2 = new HashMap<>();
    private Map<Integer, Order> book3 = new HashMap<>();

    //     void divideBook(Map<Integer, Order> orders) {
//        for (Map.Entry<Integer, Order> entry : orders.entrySet()) {
//            if (entry.getValue().book.equals("book-1")) {
//                book1.put(entry.getKey(), entry.getValue());
//            } else if (entry.getValue().book.equals("book-2")) {
//                book2.put(entry.getKey(), entry.getValue());
//            } else {
//                book3.put(entry.getKey(), entry.getValue());
//            }
//        }
//   }
    void summaryOrder(Map<Integer, Order> orders) {


    }

}
